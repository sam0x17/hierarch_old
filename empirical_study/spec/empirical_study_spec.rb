require "spec_helper"

describe EmpiricalStudy do
  it "has a version number" do
    expect(EmpiricalStudy::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
